// Định nghĩa một đối tượng actionTypes với một thuộc tính duy nhất GET_HOME. Đây là một hằng số được sử dụng làm kiểu hành động (type action) khi gửi một action trong ứng dụng.
const actionTypes = {
  GET_HOME: "GET_HOME",
};

export default actionTypes;
