// Định nghĩa một đối tượng actionTypes. Đây là một hằng số được sử dụng làm kiểu hành động (type action) khi gửi một action trong ứng dụng.
const actionTypes = {
  GET_HOME: "GET_HOME",
  SET_CUR_SONG_ID: "SET_CUR_SONG_ID",
};

export default actionTypes;
